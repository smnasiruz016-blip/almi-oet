node_modules/
.next/
dist/
*.log
.env*
.vercel
.DS_Store
.screenshots/

.vercel
.env*.local

# TypeScript incremental build cache — machine-specific, never commit
*.tsbuildinfo

.vercel
.env*

scripts/*-rollback-*.json

# Playwright run artefacts (traces, failure screenshots). The COMMITTED
# screenshots the PR links to live in docs/e2e and are written deliberately.
tests/e2e/.artifacts/
playwright-report/

# 🔴 No OET publication is ever committed here. Sources are CITED, never stored
# — see docs/sources/README.md and gate S1 in scripts/gates/sources.ts.
docs/sources/*.pdf
